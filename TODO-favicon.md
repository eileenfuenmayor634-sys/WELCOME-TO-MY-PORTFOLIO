# TODO (favicon)

- [ ] Colocar el favicon en `index.html` con `<link rel="icon" ...>` apuntando a `/favicon.png`.
- [ ] Asegurarse de que exista el archivo `public/favicon.png` (o ajustar la ruta si usas otra ubicación).

